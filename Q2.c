//Take a number(n) from user & output its cube(n*n*n).

#include <stdio.h>
int main()
{
    int n;
    printf("Enter the value of n\t");
    scanf("%d", &n);
    printf("The cube root of n is :%d", n*n*n);
    return 0;

}